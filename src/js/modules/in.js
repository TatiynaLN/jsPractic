"use strict";
function operatorIn() {
    // // Оператор in можно проверять наличие свойства в объекте.
    // let obj = { a: 1, b: 2, c: 3 };
    // console.log('b' in obj); // выведет true
    // console.log('x' in obj); // выведет false

    // //Можно удалять элементы массивов и объектов с помощью оператора delete.
    // //Давайте удалим один из элементов нашего объекта:
    // delete obj.b;
    // console.log(obj); // выведет {a: 1, c: 3}

    // // массивы
    // let arr = ['a', 'b', 'c'];
    // delete arr[1];
    // console.log(arr); // выведет ['a',, 'c']

    // let arrs = ['a', 'b', 'c', 'd', 'e'];
    // delete arrs[1];
    // delete arrs[3];
    // console.log(arrs.length);// выведет 5
    // console.log(arrs);
}

export default operatorIn;