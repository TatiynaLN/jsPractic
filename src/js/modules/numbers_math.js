"use strict";
function numbersMath() {
    // const oneNumber = 50;
    // const twoNumber = 10;
    // let result;
    // // простые математические операции
    // result = oneNumber + twoNumber;
    // console.log(result);

    // result = oneNumber - twoNumber;
    // console.log(result);

    // result = oneNumber * twoNumber;
    // console.log(result);

    // result = oneNumber / twoNumber;
    // console.log(result);

    // result = oneNumber % twoNumber; // остаток от деления
    // console.log(result);

    // /********************* MATH OBJECT = ИСПОЛЬЗУЕТСЯ ДЛЯ ВЫЧЕСЛЕНИЙ  *********************** */

    // result = Math.PI; // У MATH есть свойство PI 
    // console.log(result); // выведет число PI

    // result = Math.round(3.4); // У MATH есть метод round()- округляет
    // console.log(result); // выведет 3

    // result = Math.ceil(4.4); // У MATH есть метод ceil()- (потолок) округляет в большую сторону  
    // console.log(result); // выведет 5

    // result = Math.floor(7.9); // У MATH есть метод floor()- (пол)округляет в меньшую сторону
    // console.log(result); // выведет 7

    // // извлекаем квадратный корень
    // result = Math.sqrt(9); // квадратный корень
    // console.log(result); // 3

    // // абсолютное значение
    // result = Math.abs(-12); // дает положительное значение без знака -
    // console.log(result); // 12

    // // возведение в степень
    // result = Math.pow(2, 4); //  первым числом указывается то, что будет выводиться в степень вторым значением сама степень 2*2*2*2
    // console.log(result); // 16

    // // извлекаем минимальное значение
    // result = Math.min(2, 4, 45, 66, 88, 0, -55); // 
    // console.log(result); // выведет -55

    // // извлекаем максимальное значение
    // result = Math.max(2, 4, 45, 66, 88, 0, +99); // 
    // console.log(result); // выведет 99

    // // random - случайный, выдает случайные значения десятичных чисел с 0 до 1
    // result = Math.random(); // 
    // console.log(result); // при перезагрузке всегда разные числа

    // // random - случайный, выдает случайные значения десятичных чисел с 0 до 1 если надо увеличить значение ДО
    // result = Math.random() * 5; // 
    // console.log(result); // при перезагрузке всегда разные числа

    // // random - случайный, выдает случайные значения десятичных чисел с 0 до 1 если надо увеличить значение ДО
    // result = Math.random() + 5; // 
    // console.log(result); // первой всегда будет 5.случайные числа

    // // random - случайный, выдает случайные значения десятичных чисел, если надо округлить оборачиваем в метот огругления в большую или меньшую стороны
    // result = Math.floor(Math.random() * 100); // 
    // console.log(result); // первой всегда будет 5.случайные числа
}

export default numbersMath;